apiVersion: v2
name: cogs
version: 0.0.1
appVersion: "0.0.0"
description: default-disabled Stage 4 static render-only source shapes; rendered YAML is unsafe to apply and unproven.
type: application
annotations:
  dev.cogs/stage: "4-preparation"
  dev.cogs/security-claim: "none"
  dev.cogs/production-ready: "false"
