# Stage 3 S3-06 session Git/export evidence

## Checkpoint benchmark command

- Branch: `test/issue-68-checkpoint-benchmarks`
- Implementation base for this evidence-only slice: `aedb8e9bd5e9bfdbebade594aafd9d50f617ce6d`
- Benchmark report source SHA: emitted as `git.source_sha` by the command below for the checked-out benchmark source tree.
- Command: `npm run --silent evidence:issue68 > /tmp/cogs-issue68-benchmark.json`
- Report version: `cogs.issue68-checkpoint-benchmark/v1alpha1`
- Applicability: local functional-only
- `release_eligible`: `false`
- `isolation_authoritative`: `false`
- `aws_resources_used`: `false`

The benchmark uses actual `createGitCheckpointer`, temporary local `/usr/bin/git` repositories, and a narrow local command/SFTP adapter matching the real integration-test boundary. It does not use AWS, cloud launchers, release infrastructure, or deployment resources.

## Local benchmark results

Measured values below are from `/tmp/cogs-issue68-benchmark.json` generated by the command above. Requested limits are listed separately from measured outputs.

| case | requested maxChangedFiles | requested maxFileBytes | requested maxTotalBytes | measured file_count | measured total_bytes | result duration_ms | wall duration_ms | cleanup/invariants |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| small | 128 | 1024 | 16384 | 2 | 24 | 115 | 116 | pass |
| large-at-limit | 128 | 1024 | 131072 | 128 | 4864 | 184 | 184 | pass |
| dirty-ignored-excluded | 128 | 2048 | 32768 | 2 | 24 | 110 | 110 | pass |

For every case the script asserts:

- hidden checkpoint ref is created with the operation-returned commit;
- hidden checkpoint diff contains only eligible files;
- ignored and excluded files are absent from the checkpoint diff where applicable;
- `HEAD`, `.git/index`, and NUL `git status` bytes are identical before and after checkpointing;
- temp index and lock files are absent after cleanup;
- no remotes are configured and no push command is invoked;
- observed commands remain fixed local `/usr/bin/git -C /workspace` commands with `LC_ALL=C`;
- returned `file_count` and `total_bytes` match independently measured eligible-file counts and bytes;
- the temporary benchmark root is removed and independently confirmed absent before reporting cleanup success.

A second local benchmark run confirmed stable counts and bytes while allowing timings to vary:

| case | run 1 file_count | run 2 file_count | run 1 total_bytes | run 2 total_bytes |
| --- | ---: | ---: | ---: | ---: |
| small | 2 | 2 | 24 | 24 |
| large-at-limit | 128 | 128 | 4864 | 4864 |
| dirty-ignored-excluded | 2 | 2 | 24 | 24 |

## PR and CI evidence

PR #122 (`Add graceful shutdown preparation lifecycle`) exact head `21058821934ee5fe0ab75c0507231cba954235fe` passed:

- CI run `29580377727` / Quality and Pi embedding: success
- CI run `29580377727` / Secret scan: success
- CI run `29580377727` / Images, vulnerabilities, and SBOMs: success
- Insecure container smoke run `29580377937`: success
- KVM qualification run `29580377960` / linux-kvm: success

The PR #122 KVM run is cited only as functional/security CI evidence for that exact PR head. It is not authority for this local checkpoint benchmark and does not make the benchmark release-eligible.

## Export determinism coverage

Issue #68 export determinism remains covered by the local export test suite from the deterministic sensitive raw export slice, including durable-prefix JSONL export, canonical sidecars, manifest hashes/bytes, repeated byte-identical exports, replacement after later durable snapshots, hostile/tampered bundle rejection, cancellation/dispose behavior, authenticated API descriptor shape, absence of model-tool export, and credential absence from sidecars/descriptors.

## Limits and non-claims

- This document is evidence-only and makes no production `src/**/*.ts` changes.
- The benchmark is local functional-only and is not isolation-authoritative.
- No AWS resources are used.
- No cloud, release, deploy, or launcher claim is made.
- `release_eligible` is false.
