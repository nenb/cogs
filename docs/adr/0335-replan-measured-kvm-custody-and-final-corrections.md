# ADR 0335: Replan measured KVM custody and final corrections
- Status: Budget gate committed (`08593a43`); canonical synchronization byte replan committed (`fdf7726b`); supported-ingress denial implemented at `9ef95d29`, source-reviewed pending execution gate
- Scope: Governance/budget and implemented supported-ingress denial bookkeeping; plan-only/future-source statements below record historical gate scope, not current implementation status; no execution authority, finding closure or Docker/KVM/AWS admission
- Reviewed input: `/tmp/cogs42-kvm-budget-replan.md`; earlier protected-owner plans at `cc58ba37`, `2d8f203b` and `6bd60d3e` are superseded below
## Baseline and decision
Baseline Q remains `8ddd4c3164bae32dbe02c67d2ee9b82eb8315a38`, tree `181128aae8617eb58c5dce743416f4f69266c02c`. Reviewed pre-gate correction HEAD was `e3c1e938f3237df468ebf266f2b1cc096ae4ba05`; ADR0334 is `044e127a`. Historical pre-gate usage was 8,604/12,000 lines and 416,404/1,300,000 bytes: completion 2,047; lifecycle 2,446; relay 732; integration 3,379. These are checkpoint measurements, not current usage. The three-file Docker topology and subsequent source corrections remain unexecuted; final implementation review remains pending. Finding12 remains open because real KVM lacks ADR0333's external generation envelope.
ADR0335 is the eighth and final planned file. It supersedes ADR0334's numeric forecasts/highs, seven-file allocation and remaining-task ledger. The supported-entrypoint denial revision below supersedes the legacy launcher/insecure/evidence admission and retirement plans, including ADR0333 finding11's enabled-insecure contract and `c2439e42`'s direct-export denial requirement; it does not weaken the actual product generation owner, deferred restrictions or execution stops. Other ADR0333 contracts and exact historical owners remain unchanged. No correction, post-H, final-H, remediation or physical/conservative accounting anchor moves. **No historical gross transfers: zero lines and zero files transferred.** Existing commits gain no retrospective authority if this gate fails.
## Independent line and byte budgets
| Owner | Q baseline | New Q-relative forecast | Cumulative remediation high |
| --- | ---: | ---: | ---: |
| route | 2,023 | 0 | 2,200 |
| revocation | 2,984 | 0 | 3,000 |
| completion | 2,972 | 2,900 | 6,400 |
| lifecycle | 7,348 | 4,450 | 11,800 |
| relay | 1,953 | 2,800 | 4,800 |
| integration | 12,325 | 5,387 | 17,712 |
| total | 29,605 | 15,537 | 45,200 global |
The Q forecast endpoint is 45,142 (historically 45,055, 44,955 and 44,705), leaving 58 below the independent 45,200 global high, not transferable capacity. Owner highs sum to 45,912 (historically 45,825, 45,725 and 45,525); each owner forecast fits independently. Route/revocation forecasts remain zero, including byte/mode-identical route-index regeneration. No deletion, compression, rename/copy, net-line shortcut or ownership-transfer credit is permitted.

| Independent ceiling | High | Preserved constraint |
| --- | ---: | --- |
| correction retained / global | 31,000 / 60,000 | Deploy 24,500 and workflow 6,000 unchanged |
| post-H retained / global | 19,000 / 21,000 | Deploy 1,500 and workflow 1,200 unchanged |
| physical / conservative hard | strictly below 115,000 | Preferred 90,000 advisory; mutable owners strictly below 2,000 |
| tracked files | 1,517 | 1,509 at Q + eight planned; 1,420 remediation baseline + 97 |
| source aggregate bytes | 26,000,000 | Serialized source inventory 262,144 unchanged |
Every cap applies independently, even when another cap is tighter. Deploy/workflow/mutable-owner highs stay unchanged.

| Owner | Product Q-relative byte forecast | Cumulative remediation byte high |
| --- | ---: | ---: |
| route | 0 | 350,000 |
| revocation | 0 | 220,000 |
| completion | 300,000 | 800,000 |
| lifecycle | 450,000 | 1,200,000 |
| relay | 400,000 | 700,000 |
| integration | 2,600,000 | 3,950,000 |
| total | 3,250,000 global | 6,550,000 global |
These are independent owner byte highs, not simultaneous spend allocations or a source/test/docs split. Product owner highs sum to 3,750,000 versus global 3,250,000 (500,000 unavailable); remediation owner highs sum to 7,220,000 versus independently enforced global 6,550,000 (670,000 unavailable). Source proof uses that enforced global: 18,763,891 + 6,550,000 = 25,313,891 < 26,000,000, leaving 686,109; owner highs are not simultaneous spending authority. Historical product sums/globals 2,350,000/1,850,000, 2,161,233/1,850,000 and 2,150,000/1,850,000 (300,000 excess is not spendable global capacity) and historical source 24,733,891 remain non-spendable history. Aggregate fit does not replace actual owner byte gates or source-inventory implementation and serialized-size checks. No readiness regeneration is performed by this replan.
## Historical remaining-task ledger and review release
The 6,496 remaining gross lines at the original reviewed HEAD were allocated below. The measured denial-pivot ledger supersedes these remaining amounts and the later external-owner allocations, not historical charges or review controls. Governance, tests, docs, amendments and regeneration consume owner totals; they are not free.

| Owner | Remaining | Closed remaining tasks |
| --- | ---: | --- |
| relay | 2,068 | bounded helper/call-site/QMP/tests 1,400; review reserve 668 |
| lifecycle | 1,854 | mounted-snapshot/compose corrections 400; launcher/insecure acquisition corrections 450; production HTTPS/capability/live-mount tests 350; profile truncation 50; runtime schema/docs 50; review reserve 554 |
| integration | 1,121 | ADR0335/budget tests 500; source-inventory constant synchronization/readiness regeneration 100; later in-file ADR0335 execution-contract amendment and existing tests 200; product execution docs/tests 100; reserve 221 |
| completion | 1,453 | event review corrections/tests 700; telemetry/HTTPS integration 300; reserve 453 |

Any owner crossing stops for another reviewed gate. No reserve release before that owner's named tasks complete; release requires an explicit reviewed checkpoint identifying remaining work, task consumption, reserve use and the exact changed-since-review diff. Mechanical owner/path totals enforce budgets, not task purpose inferred from line diffs. Review controls task consumption; spare capacity is not transferable.
## Exact eight-file allocation
Integration new-file high is 87; total owner new-file highs are 97. Other highs stay route 1, revocation 0, relay 1, lifecycle 5 and completion 3. Exactly these eight paths are allocated across the entire Q-relative correction, not eight additional files:

| New path (absent from Q) | Owner |
| --- | --- |
| `dev/linux-kvm/bounded-command.py` | relay |
| `src/skills/snapshot-session-preparer.ts` | lifecycle |
| `dev/product-test/host-custody.py` | integration |
| `dev/product-test/runner.ts` | integration |
| `dev/product-test/snapshot-owner.ts` | integration |
| `docs/adr/0333-authorize-controlled-product-test-corrections.md` | integration |
| `docs/adr/0334-reallocate-measured-product-test-correction.md` | integration |
| `docs/adr/0335-replan-measured-kvm-custody-and-final-corrections.md` | integration |

All eight already exist at this pivot. No new fixture, report, restriction, schema, image or execution-gate file is implied. Tests extend existing files. The separately authorized full-check integration correction admitted only existing `test/aws-stage2-completion-kata-runtime.py` and `test/aws-stage2-completion-local-result.test.ts` for stale budget assertions; both retain their historical Q integration owner, with no runtime or workflow authority.
### Historical correction path amendments
The source-only custody/KVM amendment admitted existing `dev/linux-kvm/qualify.sh` and `test/egress-conformance/guest-probes/run-kvm-black-box-case.sh` to relay; `dev/launcher/supervisor.ts` and `test/dev-launcher-supervisor.test.ts` to lifecycle; `scripts/run-launcher-smoke-evidence.ts` and `test/launcher-smoke-evidence.test.ts` to integration. The 72-path checkpoint included the candidate-CI fixture amendment below. The later external-owner plan added six existing lifecycle paths: `dev/launcher/main.ts`, `dev/launcher/control.ts`, `dev/launcher/worker-entry.ts`, `dev/launcher/worker-process.ts`, `test/dev-launcher-control-otlp.test.ts`, `test/dev-launcher-worker-process.test.ts`, yielding 78 paths. `main.ts` had zero remediation-to-Q additions; no historical ownership moved. `c2439e42` added existing lifecycle `dev/insecure-sandbox/ssh-adapter-smoke.ts`, `dev/launcher/cli.ts`, `dev/launcher/runner.ts` (81 paths); runner retains its Q owner and the other two have zero remediation-to-Q additions. These allocations remain historical bookkeeping, not a requirement to gate internal exports or migrate tests. Earlier smoke transaction/public-inventory corrections remain non-authorizing models, not production admission.
## Finding12: separate source-only tranche, no execution authority
Finding12's source contract remains: add the bounded-command helper, convert every admitted driver/smoke/conformance guest invocation, bound QMP, reject truncation and prove behavior with portable subprocess/fault tests. It performs no Docker/KVM execution and makes no finding-closure or host-containment claim. The product scenario is Docker-only and mechanically rejects a KVM profile. Portable command bounds do not establish an external generation envelope.
**Source admission is closed now.** `dev/linux-kvm/ci-smoke.sh`, every effectful `dev/linux-kvm/driver.sh` action (including cache preparation and destroy), `dev/linux-kvm/qualify.sh` and the direct `dev/linux-kvm/bounded-command.py` CLI remain denial-only before effects. Only the driver's mechanically pure policy renderer and imported primitives for isolated portable fault models remain available. No nonce, network lease, workflow label/schedule, caller environment or Q evidence grants admission. Existing workflows remain unchanged/unadmitted. The legacy black-box entrypoint stays a denial-only stub with no transport, discovery or cleanup path; no later gate may incidentally re-enable it.
Actual KVM execution remains prohibited until focused/full source reviews pass and ADR0335 is amended in an existing-file, independently reviewed local-execution gate. That amendment must specify the complete fresh protected-runner envelope, including:
- stop-before-exec placement for every driver/smoke/helper process and cgroup escape prevention;
- all writable paths and logs, fixed storage/quota, and exact lifetime/settlement deadlines;
- the existing non-initial network namespace and root-owned `/run/cogs-kvm-network-domain/<device>-<inode>` lease;
- preloaded image/cache identity and receipt-bound retirement.
It must use existing allocated paths or stop for another budget/file gate; no ninth file is implied. Missing prerequisites fail before effects. Exact authenticated local-envelope admission at each KVM ingress, not a boolean opt-in, requires that separate review. Docker execution also still requires its separate reviewed local-execution gate.
## Historical synchronization and source-review checkpoints
Budget/source synchronization was present through `c954d817`. PR537 CI fixture corrections staged KVM replacements outside inventoried state, armed skills heartbeat loss only after preparation, bounded parse-validated SSH key retries and isolated S5 descriptors. Only existing `test/aws-stage2-completion-kata-s5.py` was additionally admitted to integration, with zero remediation-to-Q additions. Deadlines/assertions/caps and eight files stayed fixed; source-inventory limits were synchronized and readiness regenerated twice. These historical observations grant no final review or execution authority.
The subsequent unexecuted product corrections admit cgroup-v2 swappiness `null` or integer `0` only with effective `memory.swap.max=0`, retain helper leaders through both signals with pidfd `waitid(WNOWAIT)`, and fchmod/read back requested modes independently of umask. `runner.ts --capability-probes <restrictions> <candidate>` models eight fresh full-minus-one generations with non-authorizing purpose, measured pinned SSH/SFTP outcomes and exact-receipt settlement; no worker, lease, evidence admission or pass is possible. Ordinary runs require the exact full set; executable source is not execution permission.
Final-review fixes make tool errors bind evidence; snapshot discovery uses journaled publication with no external temp; Buildx inventoried retirement preserves uncertain rollback custody; FIFO nonblocking validation prevents hangs; cross-invocation fixtures serialize ancestor mutation. The follow-up to `1c859129` independently checks actual structured Bash success in runner/host and hashes the JSON-normalized admitted/native projection. Root quarantine retirement and never-unlinked OS-flock fixture models preserve foreign inodes and every safety assertion; they grant no legacy admission, finding closure or execution authority.
## Supported-entrypoint denial revision from `c2439e42`
**Product boundary:** controlled Docker orchestration is exclusively `dev/product-test/runner.ts` + `dev/product-test/snapshot-owner.ts` + root `dev/product-test/host-custody.py`, with their actual transitive production dependencies (including shared `dev/launcher/api-client.ts`, not launcher lifecycle). It does NOT use `dev/launcher/main.ts`, `dev/launcher/worker-process.ts`, `dev/insecure-sandbox/driver.sh`, `dev/insecure-sandbox/ci-smoke.sh`, `dev/insecure-sandbox/ssh-adapter-smoke.ts` or `scripts/run-launcher-smoke-evidence.ts`. No legacy handoff is needed to preserve this product route.
**Supersession:** abandon the external shared launcher/driver journal, sealed legacy actor/key/receipt/cleanup adapter, BEGIN/COMMIT delegation and worker cleanup-publication protocol proposed at `cc58ba37`/`2d8f203b`/`6bd60d3e`. Denied supported entrypoints acquire no target resources: no root ledger claim, durable retirement proof or crash-recovery promise applies to them. Existing uncertain legacy state remains untouched for separately authorized operator intervention; refusal is not cleanup, retirement or finding closure.
**Closed supported external ingress inventory:** only the following package scripts, workflow job, shell wrapper/driver operations and report CLI are legacy denial boundaries. This is a required future change, not a claim about current source; no blanket denial of every code import or callable helper is promised.

| Supported ingress | Required denial and exact future guard-test path (already allocated) |
| --- | --- |
| `package.json` launcher/insecure execution scripts | Current exact key is `launcher`; no insecure execution alias exists at `c2439e42`. Replace its body with the fixed denial command below, independent of argv/profile. `test/ci-infrastructure-boundary.test.ts` enumerates execution scripts/aliases and pre/post hooks, rejects added bypasses and any package `exports`/`bin` exposure of internal legacy modules, and proves no Node/tsx/application launch. |
| `.github/workflows/insecure-container.yml`, job `insecure-container` | Set unconditional job-level `if: ${{ false }}`: skip the whole effectful job, including checkout/setup/install, driver, Envoy/conformance, pulls, evidence and `always()`/upload steps. `test/ci-infrastructure-boundary.test.ts` parses the complete jobs map, requires exactly this disabled job, and rejects label/event/env opt-ins, step-only guards, extra effectful jobs or reusable-workflow escapes. |
| `dev/insecure-sandbox/ci-smoke.sh` | Direct and sourced entry denies before report unlink/write, receipt/report temp, dirname/git/openssl/date/tool discovery, subprocess, driver callback or cleanup trap. `test/launcher-smoke-evidence.test.ts` adds wrapper sentinel/tool-spy negatives. |
| `dev/insecure-sandbox/driver.sh` effect operations | `create`, `verify`, `reset`, `destroy` deny before setup/tools/traps or target effects, including root callers; absent/invalid action and sourced entry cannot reach effects either. `test/dev-launcher-profiles.test.ts` converts root complete-driver success assertions to denial, zero-tool calls and exact state preservation, and adds entry guards without extracting a root bypass. Both embedded Python cleanup paths and shell cleanup functions remain unreachable from these denied entries. |
| `scripts/run-launcher-smoke-evidence.ts` CLI | Deny every profile/scenario and invalid/help argv before application argument resolution, runtime/state inspection, report/export/temp effects, subprocess or cleanup registration; no effectful catch/finally report. `test/launcher-smoke-evidence.test.ts` adds executable CLI guards separately from imported helper tests. |

**Fixed package denial:** use only `sh -c 'printf "%s\n" "legacy launcher/insecure execution is disabled by ADR0335" >&2; exit 2' --`; appended npm arguments are unused shell positional arguments. Package denial does not launch Node or tsx, nor load launcher modules. Require the exact command and no pre/post execution hooks, not a forwarding script or a flag-dependent opt-in. Trusted npm/denial-shell startup is not application target resource authority; arbitrary package-manager/shell hooks or substituted executables are outside this trusted-code boundary.
**Pre-target-effect scope and tsx:** wrapper/driver/report CLI refusal emits only a fixed diagnostic/nonzero result, never an evidence report or cleanup receipt. Denial precedes target mkdir/write/unlink/rename/temp, lock/journal/authority/key mutations, resource acquisition, application subprocesses, API/SSH/network, listeners/timers and cleanup callbacks, including failure/finally paths. The directly invoked report script may start Node/tsx and its loader before script code runs: loader cache/IPC is not target resource authority and is not claimed absent. Imports and module initialization on that CLI path must not perform application target effects; deny before loading any effectful application closure. Guard tests distinguish loader-owned cache/IPC from target paths and application subprocesses; do not claim zero host effects for process startup.
**Trusted internal surfaces and guardrail:** direct TypeScript exports are trusted internal implementation/model surfaces, not package exports or supported ingress. Core/state/control/supervisor/worker/profile/trusted-compose/trusted-controls/CLI helpers, `runLauncherOperation` and exported `cleanupSensitiveExport` need no independent denial or capability retrofit. Their fake/model successes remain legitimate non-authorizing tests. Manual imports or direct invocation of internal main/worker/SSH-adapter/conformance code to execute legacy operations are prohibited by the operational guardrail; they are not mechanically blocked by an import sandbox. No resistance to arbitrary code imports or same-UID trusted-code compromise is claimed. Root/EUID, nonce, env, CLI flags, injected seams or a successful model never authorize a supported denied entry. No production bypass is added; already-retired ordinary OpenBao admission remains closed.
**Image preparation exclusion:** `scripts/prepare-launcher-images.ts` and release CI (including image build/pull/scan/SBOM steps in `.github/workflows/ci.yml`) are explicitly image preparation, not product/launcher execution, and unchanged. Their image effects are not covered by the legacy execution-denial claim and confer no execution authority. The fixed script inventory excludes image-preparation commands. No image, release workflow or lower fixture-platform amendment is admitted.
**Finding11 closure boundary:** future closure is limited to supported entrypoints plus controlled product host custody, after their implementation, exact guard tests and required reviews. It does not close arbitrary imported legacy operations or settle existing uncertain legacy state; manual imports remain prohibited by guardrail. This plan does not close finding11. Product custody below stays separate; internal success/model evidence is explicitly non-authorizing.
**Product custody unchanged:** the existing generation-bound root host custodian remains the sole product host owner for exact container/process/mount/snapshot/tool/file receipts, constructor intent, leases, deadlines, settlement and sticky uncertainty. Retain descriptor-bound quarantine, foreign-file/empty-directory preservation and awaited settlement; no new general launcher resource journal or root-ledger coverage is claimed. Product workers use the product protocol, not legacy worker-process cleanup publication. Portable models cannot establish host-root provenance or authorize execution.
**Later Docker workflow gate:** an independently reviewed protected workflow (future exact `.github/workflows/insecure-container.yml` execution gate, not this denial-only job amendment) binds exact control revision, run/attempt and candidate commit/tree/source-inventory digest on a fresh protected GitHub Linux VM, without job container/userns/unrelated workload/prior candidate execution. Credential-free pinned checkout uses `persist-credentials: false`; trusted inline bootstrap enters root via absolute admitted tools and `/usr/bin/python3 -I -B`, never a checkout verifier. Verify the full bounded exact inventory through held descriptors before installing the exact full product execution closure/root helper from the verified snapshot: runner, snapshot-owner, all transitive source/schema/data/package/dependency/Node/tsx/loader/runtime inputs, not merely the three filenames. Descriptor-bound root-owned nonroot-unwritable installation, initial-host namespace/tool/environment provenance, byte/mode/identity checks, no links/writable aliases/hooks/ambient resolution, bottom-up fsync and successful handle settlement precede use. Build the candidate worker image only from the same sealed verified context, including the fixed in-memory Dockerfile and every consumed context/ignore-rule byte; bind resulting exact image ID to candidate/closure and restrictions. Never consume a later checkout reopen/copy. Replacement or missing provenance fails before build/launch; no legacy driver/SSH adapter/evidence runner is installed. Exact closure enumeration, verifier, worker build and workflow/checker synchronization are later gate work, not implemented or authorized here. Until that independently reviewed exact gate, the whole effectful job stays unconditionally skipped; removing the job veto or restoring the old label gate is not authorized.
**Required pre-target-effect tests, future source tranche:** use exactly the three guard-test paths in the ingress matrix; `test/stage2-remediation-budget.test.ts` independently closes path ownership and docs/budget assertions. Enumerate all supported operations/profiles, malformed and valid-looking arguments/generations/receipts, root/nonroot models and direct/sourced shell entry. Preseed state/lock/authority/quarantine/report/export/temp sentinels and missing destinations; require byte/mode/device/inode-exact preservation and no new target paths. Spies require exactly zero application target-effect calls, including cleanup/failure/finally and report-CLI initialization; package guards additionally prove tsx never starts. Mutation negatives must reject moving denial after the first target effect, adding script hooks/aliases, dropping the job veto or guarding only a step; no Docker/SSH/API tools run. Root behavior uses isolated control-flow/syscall models, never sudo or real privileged execution. Loader cache/IPC is accounted separately, not confused with target sentinels.
**Preserve non-authorizing models; convert complete-driver assertions:** retain existing direct-export fake/model success tests and all generation/nonce, quarantine/foreign-inode, one-shot destroy, uncertain response, callback/finally, close/fsync and report-failure safety assertions in place. Internal imports remain allowed for these isolated tests; no new internal denial or migration of direct-export successes is required. In existing lifecycle `test/dev-launcher-profiles.test.ts`, convert the root complete-driver tests “insecure driver isolates docker tool state outside launcher controls” and “insecure driver never adopts or removes pre-existing docker competitors”, including their create/destroy assertions, to fixed denial, zero-tool calls and byte/mode/device/inode-exact state preservation with no new target paths. Preserve equivalent internal driver behavior in that file's existing isolated shell-function/embedded-Python model tests: Docker HOME/DOCKER_CONFIG/BUILDX_CONFIG isolation, no keys before build, cleanup-required intent and uncertain lock retention, hostile-state/key preservation and competitor non-adoption/non-removal; retain the static Docker context exclusion assertion. These existing model seams may be extended, but never extract or execute a complete-driver body past denial, add a root/EUID/env/flag bypass, or require sudo/real privileged execution. Models remain non-authorizing; supported-entrypoint negatives are separate from internal behavior assertions. Product portable tests continue in `test/production-compose.test.ts`; sealed-input replacement and exact-closure Linux success tests belong to the later reviewed product gate. No runtime guard tests are implemented in this plan-only commit; only existing governance assertions are synchronized.
**Exact gate-only matrix amendment:** add existing integration `package.json` and `.github/workflows/insecure-container.yml` to the Q-relative correction matrix: 83 paths, still eight previously allocated new files. Both retain historical Q integration ownership, with zero historical transfers. Admission is only for the named script/job vetoes and later separately reviewed exact product gate, not unrelated scripts or workflow execution. Exact guard-test files `test/ci-infrastructure-boundary.test.ts` and `test/launcher-smoke-evidence.test.ts` retain integration ownership; `test/dev-launcher-profiles.test.ts` retains lifecycle ownership; all three are already admitted. Historical internal-path allocations remain; only the named complete-driver assertion conversion and equivalent isolated-model preservation above are allocated, not blanket internal success-test migration or new denial requirements. Any additional path or task overrun stops for reviewed scope/fit.
**Historical denial-pivot forecast:** at `6bd60d3e`, central Q-relative gross lines are completion 2,552/3,000, relay 1,771/2,800, lifecycle 3,984/4,300, integration 4,497/5,000, total 12,804/15,100; corresponding bytes 99,893/300,000, 84,508/400,000, 187,175/450,000, 617,198/700,000, total 988,774/1,850,000. Remaining 2,296 lines: completion 448 for event/telemetry closure; relay 1,029 for separate bounded KVM closure/tests; lifecycle 316 = denial gates 90 + sentinel/model tests 180 + contingency 46; integration 503 = this governance 70 + evidence denial/tests 80 + later product execution/closure gate docs/tests 150 + regeneration 100 + contingency 103. This supersedes the external bootstrap/ledger/delegation adapter allocations, not any historical charge. No ceiling, forecast, historical transfer or reserve release changes. Charge this amendment's gross added lines/bytes without refund for removed plan prose; measure actual integrated totals independently. These bounded forecasts demonstrate governance headroom, not complete source/model/workflow implementation fit; any overrun or omitted inherited task stops for reviewed replan.
**Historical governance checkpoint at `c2439e42`:** that four-file docs/budget diff added 63 gross lines within its 70-line governance allocation and less than 26,000 raw added-line bytes. Without any refund for removed prose, that checkpoint charged integration 4,497 + 63 = 4,560/5,000 and at most 643,198/700,000 bytes; 440 lines remained under the task/review controls above. Its independent central Q-relative measurement was 12,797/15,100 lines (integration 4,490/5,000); its lower endpoint does not release the difference as spendable capacity. Local budget, ten targeted ADR0335 budget/docs tests and format/diff checks passed there; no implementation or execution test was claimed.
**Historical remaining-task ledger at `c2439e42`:** preserve the conservative integration charge 4,560/5,000 (at most 643,198/700,000 bytes), not the lower central endpoint: 440 remaining lines = this governance revision 80 + package/workflow denial and guards 50 + report CLI denial/guards 50 + later product execution/closure gate docs/tests 150 + regeneration 100 + contingency 10. Lifecycle's unchanged 316 = wrapper/driver gates 30 + separate entry sentinel tests 240 + contingency 46, with no internal test migration. Completion 448 and relay 1,029 retain their named tasks; 2,233 total lines remain before this revision. This supersedes only the prospective task allocations above, without releasing reserve or moving historical charges. All owner/global forecasts and byte/independent caps stay unchanged, total <=15,100; no reallocation between owners is needed. Charge every new governance line/byte without refund for removed prose. Complete future implementation fit is not proven; missing work or overrun requires reviewed replan, not omitted assertions or borrowed capacity.
**Historical revision governance checkpoint at `eb9a3ec0`:** that four-file plan/config/governance-assertion diff adds 64 gross lines within the 80-line revision allocation and less than 26,000 raw added-line bytes. Conservatively charge integration 4,560 + 64 = 4,624/5,000 and at most 669,198/700,000 bytes; 376 lines remain under the named task/review controls, with no deletion credit or reserve release. Independent central Q-relative usage is 12,827/15,100 lines (integration 4,520/5,000); the lower endpoint releases nothing. Local central budget, ten targeted ADR0335 governance tests, formatting, docs/matrix and diff checks pass; no runtime guard implementation, internal-test migration or execution test is claimed.
**Historical Reconciled remaining-task ledger from `eb9a3ec0`:** retain integration's conservative 4,624/5,000 lines and at most 669,198/700,000 bytes: 376 remaining = this allocation-governance amendment 26 + package/workflow denial and guards 50 + report CLI denial/guards 20 + ci-smoke wrapper sentinel/tool-spy guards 20 + later product execution/closure gate docs/tests 150 + regeneration 100 + contingency 10. Integration owns both report CLI and direct/sourced `dev/insecure-sandbox/ci-smoke.sh` guards in existing `test/launcher-smoke-evidence.test.ts`; wrapper guards include every sentinel/tool-spy and failure/cleanup negative required above. This prospectively splits the former report/guard 50 into report 20 + wrapper 20 + governance 10, plus the unspent governance 16; no reserve release. Lifecycle's unchanged 316 = wrapper/driver gates 30 + complete-driver denial conversion/entry sentinels 120 + equivalent internal driver behavior preservation in existing isolated model tests 120 + contingency 46; wrapper guard tests are not charged to lifecycle. Completion 448 and relay 1,029 retain their tasks; 2,169 total lines remain before this amendment. No lifecycle charge transfer, historical transfer, deletion credit, cap/forecast change or new file/path admission. Charge this amendment's gross additions to integration without refund; any incomplete fit or overrun stops for reviewed replan, never omitted assertions or borrowed capacity.
**Historical allocation reconciliation checkpoint at `6b54d4c7`:** this three-file plan/docs/governance-assertion diff adds 24 gross lines within the 26-line amendment allocation and less than 12,000 raw added-line bytes. Conservatively charge integration 4,624 + 24 = 4,648/5,000 and at most 681,198/700,000 bytes; 352 lines remain = governance 2 + package/workflow 50 + report CLI 20 + wrapper guards 20 + later product gate 150 + regeneration 100 + contingency 10. Lifecycle remains 3,984/4,300 with 316 available for its named tasks; no charge transfer or deletion credit. Independent central Q-relative usage is 12,841/15,100 lines (integration 4,534/5,000); the lower endpoint releases nothing. Local central budget, ten targeted ADR0335 governance tests, docs/matrix, formatting and diff checks pass; no runtime implementation or execution test is claimed.
**Historical measured failed fit and decision from `6b54d4c7`:** the formatted complete-driver conversion draft required 132/120 gross lines before completing model preservation; it was restored, leaving a clean `6b54d4c7`, with no implementation committed. Allocate conversion/entry sentinels 190 independently of model preservation 120: the old lifecycle 316 cannot cover gates 30 + conversion 190 + models 120 + contingency 46 = 386. Raise only the product total from 15,100 to 15,350 (+250): lifecycle +150 and integration +100 fund that measured shortfall and this governance without cutting assertions, other owner minima or contingency. This supersedes the prospective ledger/caps above, not historical charges; no deletion credit, ownership transfer, new path/file or execution authority.
**Historical closed remaining-task ledger from `6b54d4c7`:** preserve conservative lifecycle 3,984/4,450: 466 remaining = wrapper/driver gates 30 + complete-driver denial conversion/entry sentinels 190 + equivalent isolated internal model preservation 120 + inherited contingency 46 + withheld fit buffer 80. Preserve integration charge 4,648/5,100: 452 remaining = this governance 102 + package/workflow denial and guards 50 + report CLI guards 20 + ci-smoke wrapper guards 20 + later product execution/closure gate docs/tests 150 + regeneration 100 + inherited contingency 10. Completion 448 and relay 1,029 retain their named tasks and caps; 2,395 total remain before this amendment. Every governance addition consumes integration's 102; no refund for replaced/condensed prose, lower endpoint or unused allocation. All inherited owner minima/contingencies remain; buffers stay withheld under existing review controls.
**Independent forecast proof at `6b54d4c7`:** remediation Q baseline 29,605 + full revised product 15,350 = 44,955 <=45,000; lifecycle 7,348 +4,450 =11,798 <=11,800 and integration 12,325 +5,100 =17,425 <=17,425. Central current physical/conservative 95,358/99,461 plus the ENTIRE revised forecast again bounds them at 110,708/114,811, both strictly below hard 115,000, even double-charging already completed product work. No historical endpoint subtraction or deletion credit is used. All correction/post-H/mutable-owner gates still apply; preferred 90,000 remains advisory and exceeded.
**Historical byte fit before `e3b78585`:** central Q-relative usage is 989,430/1,850,000 bytes (integration 617,854/700,000; lifecycle 187,175/450,000). Preserve the stricter conservative integration charge of at most 681,198/700,000: cap this governance at 12,000 gross added-line bytes, leaving at least 6,802, without releasing the lower endpoint difference. Existing owner/global byte and source-inventory caps suffice for this plan-only amendment and remain unchanged; complete future implementation byte fit is not proven. Measure every later named task independently; insufficient bytes or lines require replan, never omitted assertions, compression credit or borrowed capacity. **Historical measured replan governance checkpoint at `e3b78585`:** this six-file governance-only diff charges 59/102 gross lines and at most 12,000 gross added-line bytes to integration: 4,648 +59 =4,707/5,100 and at most 693,198/700,000 bytes. Remaining 393 lines = governance 43 + unchanged named tasks/contingency 350; no deletion refund or lower-endpoint release. Central budget, eleven targeted governance tests, docs/matrix, formatting and diff checks pass; no runtime test or execution is claimed.
**Measured canonical-artifact byte replan from `e3b78585`:** canonical `docs/security-evidence/stage4-offline-readiness-artifacts/source-inventory.json` is one line, 236,550 bytes including LF. Its mandatory replacement charges the entire canonical artifact indivisibly: 236,550 - 6,802 remaining = 229,748 shortfall before implementation. No deletion credit, net-byte offset, compression, splitting/reformatting, ownership transfer or lower-central-endpoint release can make that replacement fit. Raise only integration's product Q-relative gross-added-byte high from 700,000 to 1,000,000; preserve its conservative 693,198 charge. Minimum 693,198 + 236,550 + 10,000 governance + 60,000 implementation/contingency = 999,748, rounded to 1,000,000. This supersedes only the prior prospective integration byte fit; all line caps, other owner byte highs, product global bytes 1,850,000, remediation bytes 5,970,000 and source limits remain unchanged. No artifact is regenerated here; every subsequent canonical replacement is charged in full and an additional replacement or task overrun must stop for measured replan.
**Closed byte and plan budgets:** 306,802 remaining integration bytes = canonical replacement 236,550 + this four-file governance allowance 10,000 + remaining implementation/contingency 60,252. The latter must cover all inherited integration tasks, including other readiness outputs and later gate docs/tests; complete future implementation byte fit is not proven. Conservative product charge is 99,893 completion + 187,175 lifecycle + 84,508 relay + 693,198 integration = 1,064,774; charging all 306,802 integration headroom gives 1,371,576 <=1,850,000, leaving 478,424 globally for other owners, not permission to spend every independent owner high. Remediation integration currently measures 2,080,477: even adding the entire 306,802 yields 2,387,279 <=2,700,000; total remediation 3,168,319 +306,802 =3,475,121 <=5,970,000. The stronger baseline source-byte bound stays 18,763,891 +5,970,000 =24,733,891 <26,000,000; canonical 236,550 <=262,144. Charge this plan at most 30 of the remaining 43 governance lines: conservative integration 4,707 +30 =4,737/5,100, leaving at least 363 = governance 13 + unchanged named tasks/contingency 350. No deletion refund, reserve release or line-cap change; the entire unchanged 15,350-line forecast still fits remediation 44,955/45,000 and conservative 114,811 <115,000.
**Measured denial draft failure at `504fe83e`:** restored, uncommitted `/tmp/cogs42-external-ingress-denial-draft.patch` (SHA-256 `fe5625a70ef08181e6d910c837164d8e7c6420ce0055f098961fa1c2bab443c6`) has package/workflow/guard additions 1 + 1 + 59 = 61/50 gross lines, an 11-line overrun, and 2,918 raw added-line bytes. The whole seven-existing-file draft adds 318 lines/13,020 bytes; it is failed-fit evidence, not an accepted implementation or spendable deletion credit. `/tmp/cogs42-denial-profiles.log` records two passes and one failure: “insecure Docker tool metadata is inventoried and only exact custody is retired”, diagnostic “insecure-container docker state is invalid”, expected 0, actual 1. Root cause/correction fit is unproven; do not weaken custody assertions or infer success. Clean `504fe83e` was restored; readiness regeneration and full check were not run.
**Current line decision and ledger:** product 15,450 (+100), integration 5,200 (+100), remediation integration 17,525 (+100), remediation hard 45,200 (+200; exact minimum 45,055). Retain the prior fully charged governance allowance, conservatively integration 4,737 (actual checkpoint 4,732), not the lower central 4,563: 463 remaining = this governance 102 + package/workflow denial and guards 61 + report CLI guards 20 + ci-smoke wrapper guards 20 + later product execution/closure gate docs/tests 150 + regeneration 100 + inherited contingency 10. The new 100 funds the measured 11-line shortfall and 89 governance lines; prior governance 13 stays funded. Lifecycle 3,984/4,450 retains 466 = wrapper/driver gates 30 + conversion/entry sentinels 190 + isolated model preservation 120 + inherited contingency 46 + withheld fit buffer 80. Earmark 40 of that still-withheld 80 for isolated model correction contingency, separately from preservation 120; remaining buffer 40 stays withheld. This is prospective task funding, not reserve release: correction spending requires the existing explicit reviewed task checkpoint, with measured fit and unchanged safety assertions, never a complete-driver bypass. Completion 448 and relay 1,029 retain event/telemetry and separate bounded KVM closure/tests; total remaining before this amendment is 2,406. No transfer, deletion refund, unused-allocation release or new path/file.
**Current simultaneous byte task funding:** retain integration's prior fully charged allowance of at most 703,198 bytes (actual checkpoint at most 699,936), not central 624,314. Remaining 296,802 = one indivisible canonical replacement 236,550 + this governance 15,000 + package/workflow guards 6,000 + report CLI guards 2,000 + wrapper guards 2,000 + later product gate docs/tests 20,000 + other regeneration outputs/source synchronization 12,000 + inherited contingency 3,252. Regeneration's 100 lines covers canonical replacement and all other outputs; every subsequent canonical replacement is charged in full and requires replan if unfunded. Separately fund completion event/telemetry closure/review 60,000 bytes, relay bounded KVM closure/tests/review 120,000, and lifecycle 80,000 = gates 2,000 + conversion 20,000 + preservation 18,000 + isolated correction contingency 6,000 + inherited contingency 10,000 + withheld buffer 24,000. Lifecycle's correction bytes remain gated with its 40 lines. Conservative product bytes 99,893 + 187,175 + 84,508 + 703,198 = 1,074,774; all remaining task budgets 296,802 + 60,000 + 120,000 + 80,000 give 1,631,576 <=1,850,000, leaving 218,424 globally withheld. Owner endpoints completion 159,893/300,000, lifecycle 267,175/450,000, relay 204,508/400,000 and integration 1,000,000/1,000,000 independently fit; no byte ceiling changes. These are funded forecasts, not proof of complete future implementation fit; any line/byte task overrun stops for review, never assertion cuts or borrowed capacity.
**Current independent full-forecast proof at `504fe83e`:** Q remediation 29,605 + ENTIRE product 15,450 = 45,055 <=45,200; integration 12,325 +5,200 =17,525 <=17,525; lifecycle 7,348 +4,450 =11,798 <=11,800. Central physical/conservative 95,358/99,461 plus the ENTIRE forecast again gives 110,808/114,911, strictly below unchanged hard 115,000 by 4,192/89, even double-charging completed work; remeasure after governance. This amendment and all future tasks are inside 15,450, not additional free allowances. Current remediation bytes 3,171,880 plus all 556,802 remaining task bytes =3,728,682 <=5,970,000; integration 2,084,038 +296,802 =2,380,840 <=2,700,000. Other owner byte highs independently fit; source bound remains 24,733,891 <26,000,000 and canonical 236,550 <=262,144. All correction/post-H/file/mutable-owner gates, anchors and ownership stay fixed; preferred 90,000 remains advisory and exceeded.
**Measured wrapper/report replan from `2db36ac5`:** restored draft `/tmp/cogs42-denial-implementation3-draft.patch`, SHA-256 `1004c6c90080e213f416bb300c80ed3e5078a43f9aaa8dc995a0166b90f5507a`, measures 102/40 gross lines and 4,831/4,000 raw added-line bytes across report source and existing integration guards, before remaining negatives. It is failed-fit evidence, not committed implementation or deletion credit. The draft canonicalizes the isolated model fixture with realpath while retaining alias rejection and root-stat/lstat models; uses `on_error() { finish_failure "$?"; }` / `trap on_error ERR` to avoid nested trap quoting; and gives intermediate Bash 3.2 model assertions explicit `|| exit 86` failures rather than relying on errexit. These are draft fixes, not validated source here; retain all custody/foreign-inode/uncertainty assertions, no complete-driver bypass. The reported draft conservative count 99,462 is not current implementation acceptance; readiness/full check remain unrun.
**Replacement task ledger and smallest line raise:** supersedes the three preceding “Current” ledgers/proof, now historical; prior commit charges integration 4,798 lines/716,683 bytes, retaining earlier fully charged allowances, not central 4,582/630,911. Its remaining governance 41 lines/1,515 bytes stays governance, not task contingency. Wrapper/report minimum becomes 130 lines/8,000 bytes, including 28 lines/3,169 bytes beyond the measured partial draft for remaining negatives and task contingency. Reallocate only existing integration contingency 10 lines/3,252 bytes prospectively to this named task; no general reserve release or cross-owner transfer. The 90-line increase cannot fit that 10; measured governance needs 48/41 lines, so smallest product raise +87 to 15,537, integration +87 to 5,287, remediation integration +87 to 17,612; remediation global stays 45,200. Remaining integration 489 = this governance 48 + package/workflow 61 + combined wrapper/report 130 + later product gate 150 + regeneration 100; unassigned integration contingency is now zero. All lifecycle/completion/relay minima and withheld reserves remain unchanged, including separately gated model correction 40 lines/6,000 bytes. No unused allowance or lower endpoint refund. Any remaining-negative or governance overrun stops again, never cuts assertions.
**Replacement simultaneous byte funding and independent proof:** governance is bounded at 12,000 bytes (prior 1,515 plus 10,485), covering the measured 10,804-byte preliminary diff with contingency; task increase 4,000 minus integration contingency 3,252 leaves 748, so the smallest integration byte raise for these funded allowances is 11,233 to 1,011,233. Remaining 294,550 = governance 12,000 + package/workflow 6,000 + wrapper/report 8,000 + later gate 20,000 + canonical replacement 236,550 + other regeneration 12,000; generic contingency zero, task contingency included above. Product task endpoint 1,631,576 +11,233 =1,642,809 <=1,850,000; other owner byte endpoints stay 159,893/267,175/204,508. Pre-amendment remediation bytes 3,178,477 +294,550 +260,000 =3,733,027 <=5,970,000; integration 2,090,635 +294,550 =2,385,185 <=2,700,000. Q lines 29,605 +ENTIRE product 15,537 =45,142 <=45,200; integration 12,325 +5,287 =17,612. Governance measurement physical/conservative 95,359/99,462 plus ENTIRE forecast gives 110,896/114,999 <115,000, with only one conservative line to spare; remeasure exact final diff. Source 24,733,891 <26,000,000, canonical <=262,144, every other cap/anchor and 83 paths/eight files remain fixed. These forecasts fund remaining negatives, not proof of complete implementation fit.
**Immutable canonical lower bound from `086b2c2c`:** `/tmp/cogs42-denial-implementation4.md` SHA-256 `2c402ebbd5cc8d4ea2e0efcf0a4561ff6ba4a72d61dab74db4ab5c85c594708a` records restored draft `/tmp/cogs42-denial-implementation4-draft.patch`, SHA-256 `403baa5740442a6afe608f701f33635c7dc00561d90e1f6022f0e5814f7e07f2`: 466 gross lines/20,673 bytes across eight existing files, 46 targeted tests passed, no accepted implementation. Existing canonical local-validation 25,249 + package 18,687 = 43,936 bytes including LF, a 31,936 shortfall against synchronization 12,000 before other outputs/source anchors. Each is one indivisible canonical line: local-validation SHA-256 `8ff3d8daad3fecfc7a8c20b5c7dc68ce4df88a8eb2f39db68fae548c3d508b24`, package `42ca1e7c043dda78e5942cf58dd5e14ebac6798aa263db879f76d87ab28419b1`. Local-validation must replace package binding `6413d9e06ca6888ed2b5a43b1026bee81209e8ca50e4d7a177a2b59d510de940` with draft package digest `9dc27e8044a17b33402f68b54834dc7ff4bee798a936a2ca08c5892da37b25cb`; package must rebind inventory/local-validation digests. These are immutable pre-regeneration lower-bound measurements, NOT measured regenerated totals; readiness regeneration/full check were not run. The separate source-inventory canonical replacement remains 236,550 bytes. No splitting/reformatting, compression, deletion/net-byte credit, ownership transfer or lower-endpoint release; every actual canonical replacement charges its full raw added-line bytes and any additional replacement/overrun stops for reviewed replan.
**Replacement exact canonical byte funding:** supersedes the preceding replacement ledger/proof prospectively, retaining all historical charges. `086b2c2c` charged 48/48 governance lines and 11,092/12,000 bytes: conservative integration 4,846 lines/727,775 bytes plus unused prior governance 908 bytes withheld = fully charged 728,683, not central 4,593/636,081. Raise other regeneration outputs/source synchronization from 12,000 to 60,000 bytes (+48,000), exclusively for mandatory canonical local-validation/package and all other synchronization outputs/source anchors; 60,000 -43,936 =16,064 is unmeasured task margin, not free contingency. Separately fund this governance at 12,000 bytes, without consuming that 60,000 or releasing the 908. Remaining named bytes 342,550 = governance 12,000 + package/workflow 6,000 + wrapper/report 8,000 + later product gate 20,000 + source-inventory 236,550 + synchronization 60,000. Integration endpoint 728,683 +342,550 =1,071,233 exceeds historical 1,000,000 and current 1,011,233; raise integration high to 1,200,000 (+188,767 from current), leaving 128,767 withheld/unallocated, not task or reserve-release authority. Other funded endpoints remain completion 159,893, lifecycle 267,175, relay 204,508: simultaneous 1,702,809 <= unchanged product 1,850,000, leaving 147,191 globally withheld. Even all integration headroom yields 1,831,576 <=1,850,000 with those other funded tasks, not their independent highs. Baseline remediation bytes 3,183,647 +342,550 +260,000 =3,786,197 <=5,970,000; integration 2,095,805 +342,550 =2,438,355 <=2,700,000 (even all 471,317 integration headroom yields 2,567,122). Source bound remains 24,733,891 <26,000,000; serialized inventory <=262,144. No line cap changes: integration 5,287, product 15,537, remediation integration/global 17,612/45,200; every task cap, other owner minimum/reserve, path/file/anchor and independent cap stays fixed. The existing 100-line regeneration/source-synchronization task now explicitly includes this governance budget synchronization (at most 40 gross lines), leaving at least 60 for later canonical/source outputs within the SAME 100, not a new allowance or transfer from another task. Keep package/workflow 61, wrapper/report 130 and later gate 150: 4,846 +61 +130 +150 +100 =5,287. Charge actual governance additions in the commit, retain unused governance withheld; later exact output fit must be measured, never omit work or borrow capacity. Full line forecast remains 45,142; physical/conservative 95,359/99,462 +15,537 =110,896/114,999 <115,000, with one conservative line of margin; remeasure final diff.
**Historical retained-byte funding from `3e996928`:** supersede prior prospective byte ledgers only. Preserve measured retained integration 1,312,514 product / 2,679,636 remediation, not lower central endpoints 653,443 / 2,112,012: starting product 728,683 / remediation 2,095,805 at `086b2c2c`, charge successive committed integration additions 11,740 (`fdf7726b`), 290,399 (`9ef95d29`) and 281,692 (`3e996928`), including every repeated canonical replacement. Fund two distinct complete canonical replacement bundles at 300,000 each (source inventory plus local-validation/package and other synchronization outputs per bundle), inherited later gate 20,000, inherited withheld 4,517 and separate new governance 20,000: future integration 644,517; endpoints 1,957,031/2,000,000 and 3,324,153/3,350,000. The 600,000 replaces the prospective 236,550+60,000 regeneration allocation, never refunds previous replacements; every actual canonical replacement is charged in full, even identical repeated canonical charges, with no refund, splitting, compression, deletion/net-byte credit or lower-endpoint release. Preserve completion/lifecycle/relay funded endpoints 159,893/267,175/204,508 (all below 300,000/450,000/400,000); simultaneous product 2,588,607/2,650,000, and even all integration headroom gives 2,631,576/2,650,000. Remediation endpoints route 96,627/350,000, revocation 114,977/220,000, completion 288,357/800,000, lifecycle 558,274/1,200,000, relay 297,404/700,000 conservatively add the entire inherited 60,000/80,000/120,000 again to current central non-integration usage; total with integration 4,679,792/6,550,000, or 4,705,639 with all integration headroom. Rounded ceilings are product integration/global 2,000,000/2,650,000 and remediation integration/global 3,350,000/6,550,000; unused headroom and governance stay withheld. All line caps unchanged, including regeneration 100, later gate 150, product 15,537/integration 5,287 and remediation 45,200/integration 17,612. Retained integration lines are 4,846+33+195+9=5,083; new governance is bounded at 44 gross lines within the same synchronization task, preserving later gate 150, unused wrapper/report 4 withheld and two minimum three-line canonical bundles: 5,083+44+150+4+6=5,287. Both later bundles still require exact fit; this six-line floor is not proof that every later source-anchor/output change fits. Historical whole-forecast double-charge 99,462+15,537=114,999 is not a current proof: current conservative 99,463 plus that entire forecast equals the strict 115,000 boundary and cannot be claimed passing. Use the independently measured actual gates; stop on any future overrun, never release retained charges or cut assertions.
**Current retained replan from `e4b7835c`:** supersede only prospective funding/highs above. Committed integration bytes: product 1,312,514 +20,000 fully charged prior governance (12,478 actual; 7,522 withheld) +280,750 =1,613,264; remediation 2,679,636 +20,000 +280,750 =2,980,386. The distinct overwritten initial refresh evidenced by the initial/corrected manifests in `/tmp/cogs42-final-evidence.FUpXD6` is not refunded: hold another 300,000 bytes and five lines for its full three canonical outputs plus two classifier anchors, separately from committed final output charges. This is conservative historical funding, not a measured overwritten-byte total or retrospective implementation approval; unresolved excess stops further work. Retained funded bases are therefore 1,913,264/3,280,386 bytes and 5,134+5=5,139 lines, never the lower central endpoints. Identical stability comparisons do not refund any replacement; all distinct replacements remain charged.
**Current smallest rounded byte decision:** fund two ADDITIONAL complete bundles at 300,000 each, later gate 20,000, inherited withheld 4,517, new governance 20,000 and exact-tags correction/tests 8,000: 652,517 future bytes. Integration endpoints 2,565,781 product /3,932,903 remediation; retain other product funded endpoints completion/lifecycle/relay 159,893/267,175/204,508 and remediation route/revocation/completion/lifecycle/relay 96,627/114,977/288,357/558,274/297,404. Simultaneous product 3,197,357 and remediation 5,288,542; with full integration headroom 3,231,576/5,305,639. Round to the smallest 50,000-byte multiples funding these endpoints and full integration headroom: product integration/global 2,600,000/3,250,000 (above requested floors 2,300,000/2,950,000), remediation integration 3,950,000; keep global 6,550,000. All unused allowances stay withheld. Source bound is still 18,763,891+6,550,000=25,313,891 <26,000,000; serialized inventory <=262,144. Every future bundle includes source inventory, local-validation/package and other outputs/anchors; exact line/byte fit remains mandatory, no split/compression/deletion credit.
**Current 100-line completion-to-integration reallocation and exact empty-tags plan:** prospectively move 100 lines, never historical gross: product completion 3,000->2,900 and integration 5,287->5,387; remediation completion 6,500->6,400 and integration 17,612->17,712. Product/global 15,537 and remediation/global 45,200 stay fixed; Q full forecast remains 45,142, owner high sum 45,912. Completion retains 2,552 charged +348 event/telemetry closure lines; its unchanged 60,000-byte funding and all other owner minima/reserves remain. Synchronization task 100->200 includes its historical 101 committed +5 overwritten lines, leaving 94 = this governance 64 + deferred exact-tags correction/tests 20 + two canonical bundles/anchors 10. Integration 5,139+64+20+10+150 protected later gate+4 withheld wrapper/report =5,387. This replaces the failed old 100-line fit, not a reserve release; charge the final governance diff and withhold unused allowance. Future source only in existing integration `dev/product-test/host-custody.py` and `test/ci-infrastructure-boundary.test.ts`: admit exactly `.git/refs/tags/` as an empty directory with inventory value `None`, never a file, symlink, tag ref, descendant or broad regex alternative. Keep all existing custody/evidence checks. Add a real local Git fixture using the runner's exact init/config/commit/notes commands and production inventory/evidence validation, plus negatives for nonempty tags, nested descendants, file/symlink substitution and wrong inventory value; preserve existing hostile tests, no fabricated-only positive or tag-name wildcard. No implementation/readiness here; any exact task fit failure stops for replan, not omitted tests. Physical/conservative gates must be measured independently; 99,463+15,537=115,000 is NOT a passing whole-forecast proof.
**This revision's validation/commit boundary:** explicit owner instruction authorizes this plan-only commit in only this existing ADR (<=130 lines), `docs/adr/README.md`, `config/external-review-remediation-budget-v1.json`, numeric/schema assertions in `scripts/check-stage2-retained-lines.py` and existing governance assertions in `test/stage2-remediation-budget.test.ts` plus numeric budget assertions in existing `test/aws-stage2-completion-local-result.test.ts`. No accounting algorithm changes: raw gross measurement remains unchanged; source forecast now uses the independently enforced global, not the non-simultaneous owner sum. Historical `2db36ac5` charged 61 of its 102 lines/15,000 bytes; no refund. Run governance/budget/docs and format/diff checks, record actual governance consumption in the commit, retain unused allowance withheld. Historical policy required two independent governance reviews of the exact final diff; explicit owner instruction supersedes that pre-commit condition only for this plan commit, with no independent review success claimed. No source implementation, new files, readiness regeneration, reserve release, full check, finding closure, Docker/KVM/AWS/provider execution, workflow dispatch or authoritative H/G/Q operation.
The checker/tests enforce actual Q-relative owner line+byte highs, global and every independent cap, exact eight-file allocation, no ownership transfer/deletion credit and hostile central/attribute bypasses. Preserve raw UTF-8 added-line bytes including actual LF/CRLF, full canonical generated-line charges, ordinary untracked-file charges, NUL/UTF-8/ordinary-file checks, isolated Git configuration/attributes, uncached index, repository-transform rejection, Myers/indent/U3 patch selection and numstat cross-check. No filter, textconv, rename/copy or staged-clean-output bypass; ADR0334 accounting remains applicable.
The original governance gate changed only this ADR, `docs/adr/README.md`, the ADR0334 supersession reference, budget config/checker/tests. Its instruction was “Do not commit pending two independent reviews”; committed budget status does not establish final source review. The current revision's smaller allowlist is explicit above. No AWS/provider/model/credential operation or authoritative H/G/Q workflow is authorized; no retirement or evidence relabeling. **Stop immediately before every AWS-facing command.**
