# ADR 0335: Replan measured KVM custody and final corrections
- Status: Budget gate committed (`08593a43`); external protected-owner correction replanned from `cc58ba37`, not implemented; final implementation review and local execution pending
- Scope: Governance/budget gate only; no product source or execution authority
- Reviewed input: `/tmp/cogs42-kvm-budget-replan.md`
## Baseline and decision
Baseline Q remains `8ddd4c3164bae32dbe02c67d2ee9b82eb8315a38`, tree `181128aae8617eb58c5dce743416f4f69266c02c`. Reviewed pre-gate correction HEAD was `e3c1e938f3237df468ebf266f2b1cc096ae4ba05`; ADR0334 is `044e127a`. Measured Q-relative gross usage before this gate is 8,604/12,000 lines and 416,404/1,300,000 bytes: completion 2,047; lifecycle 2,446; relay 732; integration 3,379. These are the reviewed checkpoint measurements, not measurements after this ADR's additions. The three-file Docker functional topology implementation and subsequent review corrections are present but unexecuted; final implementation review remains pending. Finding12 remains open because real KVM lacks the external generation envelope required by ADR0333.
Add this governance-only ADR as the eighth and final planned file. This supersedes ADR0334's numeric forecasts/highs, seven-file allocation and remaining-task ledger, and clarifies ADR0333 finding12's source-only tranche and later execution stop. ADR0333's path matrix, production contracts, deferred restrictions and all other stops remain unchanged. No correction, post-H, final-H, remediation or physical/conservative accounting anchor moves. **No historical gross transfers: zero lines and zero files transferred.** Existing commits gain no retrospective authority if this gate fails.
## Independent line and byte budgets

| Owner | Q baseline | New Q-relative forecast | Cumulative remediation high |
| --- | ---: | ---: | ---: |
| route | 2,023 | 0 | 2,200 |
| revocation | 2,984 | 0 | 3,000 |
| completion | 2,972 | 3,000 | 6,500 |
| lifecycle | 7,348 | 4,300 | 11,700 |
| relay | 1,953 | 2,800 | 4,800 |
| integration | 12,325 | 5,000 | 17,325 |
| total | 29,605 | 15,100 | 45,000 global |

The Q forecast endpoint is 44,705, not permission to transfer spare capacity up to the 45,000 global high. Owner highs now sum to 45,525 (previously 45,200); the global 45,000 remains an independent stop. Route/revocation forecasts remain zero, including byte/mode-identical route-index regeneration. No deletion, compression, rename/copy, net-line shortcut or ownership-transfer credit is permitted.

| Independent ceiling | High | Preserved constraint |
| --- | ---: | --- |
| correction retained / global | 31,000 / 60,000 | Deploy 24,500 and workflow 6,000 unchanged |
| post-H retained / global | 19,000 / 21,000 | Deploy 1,500 and workflow 1,200 unchanged |
| physical / conservative hard | strictly below 115,000 | Preferred 90,000 advisory; mutable owners strictly below 2,000 |
| tracked files | 1,517 | 1,509 at Q + eight planned; 1,420 remediation baseline + 97 |
| source aggregate bytes | 26,000,000 | Serialized source inventory 262,144 unchanged |

Every cap applies independently, even when another cap is tighter. Deploy/workflow/mutable-owner highs stay unchanged.

| Owner | Product Q-relative byte forecast | Cumulative remediation byte high |
| --- | ---: | ---: |
| route | 0 | 350,000 |
| revocation | 0 | 220,000 |
| completion | 300,000 | 800,000 |
| lifecycle | 450,000 | 1,200,000 |
| relay | 400,000 | 700,000 |
| integration | 700,000 | 2,700,000 |
| total | 1,850,000 | 5,970,000 |

These are exact owner totals, not a new source/test/docs category split. Baseline source bytes 18,763,891 + cumulative byte highs 5,970,000 = 24,733,891, below 26,000,000. Aggregate forecast fit does not replace actual owner byte gates or later independent source-inventory implementation and serialized-size checks. Source-inventory constant synchronization and deterministic readiness regeneration were mandatory follow-up work, not performed or claimed by the original budget gate.
## Historical remaining-task ledger and review release
The 6,496 remaining gross lines at the original reviewed HEAD were allocated below; the `cc58ba37` replan's measured remaining-work forecast below supersedes these remaining amounts, not historical charges or review controls. Governance, tests, docs, later amendments and regeneration consume owner totals; they are not free.

| Owner | Remaining | Closed remaining tasks |
| --- | ---: | --- |
| relay | 2,068 | bounded helper/call-site/QMP/tests 1,400; review reserve 668 |
| lifecycle | 1,854 | mounted-snapshot/compose corrections 400; launcher/insecure acquisition corrections 450; production HTTPS/capability/live-mount tests 350; profile truncation 50; runtime schema/docs 50; review reserve 554 |
| integration | 1,121 | ADR0335/budget tests 500; source-inventory constant synchronization/readiness regeneration 100; later in-file ADR0335 execution-contract amendment and existing tests 200; product execution docs/tests 100; reserve 221 |
| completion | 1,453 | event review corrections/tests 700; telemetry/HTTPS integration 300; reserve 453 |

Any owner crossing stops and requires another reviewed gate. No reserve release before that owner's named tasks complete; release requires an explicit reviewed checkpoint identifying remaining work, task consumption, reserve use and the exact changed-since-review diff. Named-task consumption and reserve release are controlled by code review and changed-since-review checkpoints. Owner line/byte gates are mechanical; the checker truthfully enforces owner/path totals, not task purpose inferred from line diffs. Review corrections must stay within the named allocations and authorized releases; spare capacity is not transferable.
## Exact eight-file allocation
Integration new-file high becomes 87; total owner new-file highs become 97. Other highs stay route 1, revocation 0, relay 1, lifecycle 5 and completion 3. Exactly these eight paths are allocated across the entire Q-relative correction, not eight additional files in this gate:

| New path (absent from Q) | Owner |
| --- | --- |
| `dev/linux-kvm/bounded-command.py` | relay |
| `src/skills/snapshot-session-preparer.ts` | lifecycle |
| `dev/product-test/host-custody.py` | integration |
| `dev/product-test/runner.ts` | integration |
| `dev/product-test/snapshot-owner.ts` | integration |
| `docs/adr/0333-authorize-controlled-product-test-corrections.md` | integration |
| `docs/adr/0334-reallocate-measured-product-test-correction.md` | integration |
| `docs/adr/0335-replan-measured-kvm-custody-and-final-corrections.md` | integration |

Only ADR0335 is created now. No new fixture, report, restriction, schema, image or execution-gate file is implied. Existing ownership remains exact and unique, including all historical Q allocations. Tests extend existing files.
The separately authorized full-check integration correction admits only `test/aws-stage2-completion-kata-runtime.py` and `test/aws-stage2-completion-local-result.test.ts` to the product matrix as existing integration paths. Both retain their historical Q integration owner; only stale budget assertions change, with no runtime or workflow authority. Budget/test corrections use the existing integration ADR0335/budget-test allocation; source-inventory synchronization and deterministic regeneration use the existing 100-line allocation. No ceiling, new-file allocation, reserve release or historical gross transfer changes.
### Authorized custody/KVM review correction path amendment
The owner's source-only review correction adds exactly these existing paths to the product matrix, retaining their historical Q owners:

| Existing path | Owner |
| --- | --- |
| `dev/linux-kvm/qualify.sh` | relay |
| `test/egress-conformance/guest-probes/run-kvm-black-box-case.sh` | relay |
| `dev/launcher/supervisor.ts` | lifecycle |
| `test/dev-launcher-supervisor.test.ts` | lifecycle |
| `scripts/run-launcher-smoke-evidence.ts` | integration |
| `test/launcher-smoke-evidence.test.ts` | integration |

This checkpoint's product matrix contained 72 paths, still exactly eight new files, including the candidate-CI fixture amendment below; the external-owner amendment below adds six existing paths. No ceiling, forecast, reserve release or historical gross transfer changes. Relay changes/tests consume the bounded-helper/call-site allocation; lifecycle inventory changes/tests consume launcher acquisition corrections; matrix synchronization consumes integration budget tests, and exact inventory-consumer validation consumes product execution docs/tests. Smoke consumes destroy authority before invocation, fails on nonzero/malformed retirement and never retries after report failure. Public inventory exposes acquisition uncertainty and retirement intent independently of mutable phase and worker recovery, without conferring cleanup authority.
## Finding12: source-only tranche, no execution authority
Finding12 implementation in the later source tranche is source-only: add the bounded-command helper, convert every admitted driver/smoke/conformance guest invocation, bound QMP, reject truncation and prove behavior with portable subprocess/fault tests. It performs no Docker/KVM execution and makes no finding-closure or host-containment claim. The initial functional product-test topology is Docker-only and mechanically rejects a KVM profile, so finding12 cannot be triggered by that product scenario. Portable command bounds do not establish an external generation envelope.
**Source admission is closed now.** No exact local KVM execution authorization is issued. `dev/linux-kvm/ci-smoke.sh`, every effectful `dev/linux-kvm/driver.sh` action (including cache preparation and destroy), `dev/linux-kvm/qualify.sh`, and the direct `dev/linux-kvm/bounded-command.py` CLI deny before effects. Only the driver's pure policy renderer and imported primitives used by portable fault tests remain available. No nonce, network lease, workflow label/schedule, caller environment value or Q evidence satisfies this missing authorization. Existing workflows are unchanged and cannot execute these KVM paths. The legacy black-box entrypoint is a denial-only stub with no transport, discovery or cleanup path; lifting other gates must not re-enable it. The later amendment must replace these source stops with exact authenticated local-envelope admission at every ingress, not a boolean opt-in. This correction is not that execution gate.
Actual KVM execution remains prohibited until focused/full source reviews pass and ADR0335 is amended in an existing-file, independently reviewed local-execution gate. That later in-file amendment must specify the complete fresh protected-runner envelope then used, including:
- stop-before-exec placement for every driver/smoke/helper process and cgroup escape prevention;
- all writable paths and logs, fixed storage/quota, and exact lifetime/settlement deadlines;
- the existing non-initial network namespace and root-owned `/run/cogs-kvm-network-domain/<device>-<inode>` lease;
- preloaded image/cache identity and receipt-bound retirement.
It must use existing allocated paths or stop for another budget/file gate; no ninth file is implied. Missing prerequisites fail before effects. The present ADR is not that amendment. Docker execution also still requires the separate reviewed local-execution gate; an unexecuted topology or portable test cannot substitute for it.
## Current synchronization checkpoint
The budget gate was committed in `08593a43`; source corrections and readiness are present through `c954d817`. PR537 candidate CI exposed fixture assumptions: KVM replacement now stages outside inventoried state before atomic rename (distinct live inode; inode ABA remains possible after unlink/recreate); skills heartbeat loss arms only after successful preparation; both SSH fixture keys use bounded parse-validated retries; S5 descriptor collisions use private regular files rather than shared `/dev/null` timestamps. Admit only existing `test/aws-stage2-completion-kata-s5.py` to integration's product matrix and allocation; it has zero remediation-to-Q gross additions, so no historical ownership transfer. Compact prior-added integration test scaffolding without removing coverage to retain 4,500 gross lines; preserve all deadlines, assertions, caps and the eight-file allocation. Source-inventory limits stay synchronized and readiness is regenerated twice. These fixture corrections do not complete final implementation review or authorize execution. Finding12 remains open; Docker/KVM/AWS/provider stops and the independently reviewed local-execution gate remain pending.
The remaining product-review corrections accept Docker's cgroup-v2 swappiness `null` or integer `0` only while still checking effective `memory.swap.max=0`; retain helper leaders with pidfd `waitid(WNOWAIT)` through both final signals before reap; and fchmod/read back requested directory modes independently of umask. Portable regressions cover ordering/reuse and each executable capability-removal create/start/probe/receipt-cleanup contract. The source-only `runner.ts --capability-probes <restrictions> <candidate>` entry runs eight fresh generations, each with exactly full-minus-one capabilities and a non-authorizing custody purpose: startup and pinned authenticated SSH/SFTP outcomes are measured, not presumed failures. No probe can create a worker, grant a lease, admit evidence or pass; output follows exact-receipt settlement, and uncertainty remains failed. The ordinary run still requires the exact full set. This implements executable scenarios, not permission to execute them; all existing Docker/KVM/AWS stops remain closed.
### Final-review fix checkpoint
Final-review fixes make tool errors bind evidence; snapshot discovery uses journaled publication with no external temp; Buildx inventoried retirement removes only exact tool metadata and preserves uncertain rollback custody; FIFO nonblocking validation prevents hangs; and cross-invocation fixtures serialize ancestor mutation without weakening trusted-file validation. This checkpoint grants no execution authority; all Docker/KVM/AWS/provider and independently reviewed local-execution gates remain closed.
The follow-up to `1c859129` independently admits the actual bounded structured Bash success in runner and host (`ok` true, integer exit code zero, exact output/error-accounting shape); Pi's `isError` alone grants nothing. Hashes use the exact JSON-normalized admitted/native projection, including omission of own `usage:undefined`, with success/failure regressions through the real Bash/Pi adapter. Buildx retirement requires a descriptor-held root-owned private parent and private quarantine: rename, verify the moved inode/content, then delete only within that protected namespace. Non-root retirement and any mismatch fail closed, preserve custody, and never invoke sudo; portable root syscall models test final unlink/rmdir replacements and quarantined foreign inodes. The fixture uses a never-unlinked OS flock inode with bounded acquisition and pipe/kill release; killed-holder and serialization controls remain local-only. AST-identical statement compaction preserves every host assertion and branch within the unchanged integration ceiling. No new files, budget changes, execution authority, or Docker/KVM/AWS/provider actions are introduced.
## External protected-owner correction plan from `cc58ba37`
Reviewed inputs: `/tmp/cogs42-retire-plan-review-a.md` passes the prior plan; `/tmp/cogs42-retire-plan-review-b.md` identifies four HOLD gaps. This plan supersedes the in-process root ledger/scoped bypass and independent nested driver histories: one external protected owner in existing `dev/product-test/host-custody.py` owns production admission, the shared generation transaction and retirement. No implementation closure or execution authority is granted.
- **HOLD 1 — concrete bootstrap:** only a fresh protected GitHub-hosted Linux VM, with no job container, userns setup, unrelated workloads or prior candidate execution, is admissible. The independently reviewed protected workflow body (future existing `.github/workflows/insecure-container.yml` gate, not its current PR path) binds its exact control revision, run/attempt and approved exact candidate commit/tree/source-inventory digest outside the candidate checkout. Pinned checkout uses that exact commit and `persist-credentials: false`; no checkout credentials, agents, secrets, hooks, package scripts or candidate startup code reach bootstrap. Workflow labels, caller env and self-attested manifests grant nothing; current workflows remain unadmitted and unchanged.
- **Root installation:** the protected workflow alone enters root through absolute root-owned `/usr/bin/sudo -n /usr/bin/env -i`, running its inline trusted verifier with `/usr/bin/python3 -I -B`; never execute a checkout verifier first. Root descriptor-captures and verifies the complete bounded exact source inventory (literal paths, ordinary types, modes, lengths, hashes, no extras/links/mutation) against the protected candidate binding, then copies the fixed privileged closure, exactly `dev/product-test/host-custody.py` and `dev/insecure-sandbox/driver.sh`, into a freshly exclusive root-owned `/opt/cogs-product-custody/<candidate>-<nonce>` subtree. Driver data/build inputs must match that inventory; no host imports/sourcing/plugins or executable checkout fallback. Verify copied bytes/modes, fsync files and directories bottom-up through `/opt`, and close successfully before launch; never adopt/chown old nonroot custody.
- **Host identity/interpreters:** require Linux kernel EUID 0, equal device/inode identities for `/proc/self/ns/user` and `/proc/1/ns/user`, and the exact initial `/proc/self/uid_map` tuple `(0, 0, 4294967295)`. These checks are necessary only within the authenticated fresh-VM bootstrap above, not standalone host-root proof; missing APIs/provenance or namespace mismatch refuses. No userns/container root claim, `NS_GET_PARENT`/EPERM inference or malicious-root resistance. Root validates absolute root-owned, nonroot-unwritable tools, loaders and Python stdlib; execute only `/usr/bin/python3 -I -B <installed-helper>` and `/usr/bin/bash --noprofile --norc <installed-driver>`, never the env shebang. Start with empty env plus fixed `PATH=/usr/sbin:/usr/bin:/sbin:/bin`, `LC_ALL=C`, `HOME=/nonexistent`, `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_OPTIONAL_LOCKS=0`; exclude `BASH_ENV`, `ENV`, `PYTHON*`, `LD_*`, Node hooks and caller tool/config selectors. All subordinate commands use admitted absolute tools and closed custodian-derived arguments/environment; no script self-elevation.
- **Protection from creation:** descriptor-anchor UID-0, nonroot-unwritable ancestry from trusted `/` through installation, persistent journal, state, authority/control files, temporary cleanup and quarantine. Private directories are 0700, executable closure 0500, immutable inputs 0400, journal/control records 0600; fchmod/read back independent of umask and fsync before exposure. Serialize trusted writers; reject ACL/mount exceptions, renameable ancestors, inherited nonroot-writable handles and guest/workload writable aliases. Root owns every authoritative write; launcher/worker clients cannot turn observations or process-local objects into filesystem authority.
- **HOLD 2 — pre-effect order:** `main.ts` and `resolveLauncherState` first perform read-only coordinate resolution, with no `privateChild`, `ensureRoot`, mkdir, lock mutation, revision subprocess or callback/spawn. Root validates canonical launcher AND driver locators, held protected parents/state identities or explicit pre-creation absence; a stable shared resource key excludes aliases and does not change with generation, source revision or install nonce. The custodian replays external history, exclusively reserves the next slot and durably writes BEGIN before any main/state mkdir, launcher/worker/helper spawn or driver effect: fsync the record and all new directory entries/parents, then settle their handles. The sole pre-BEGIN bootstrap exception is trusted verifier/custodian startup, installation and the external journal's own protected ancestry/reservation/record creation; it creates no lifecycle state or runs lifecycle tools. Missing journal/durability capabilities refuse there, not after acquisition.
- **External exclusion/replay:** history lives under persistent root-owned `/var/lib/cogs-product-custody/journal/<resource-key>`, outside every state/lock/quarantine/install-removal subtree, never `/run` or reboot-volatile storage. One exclusive root custodian uses a never-unlinked protected OS-lock inode to hold shared-resource admission through terminal publication; lock order is external resource exclusion, slot reservation/BEGIN, then subordinate state/driver locks, released in reverse with external exclusion last. Closed versioned records bind canonical coordinates, protected identities/absence, exact candidate, generation, sequence and retirement custody. Literally enumerate the complete ordered history (maximum 1024 slots, 64 KiB per record; overflow refuses); validate every surviving slot/custody. Empty reservation, pending, torn, malformed, foreign, contradictory, duplicate or legacy-uncertain history vetoes all ordinary reuse and driver effects; older COMMIT never masks a newer reservation. Never unlink/truncate/overwrite/compact history or infer settlement from absence.
- **HOLD 3 — shared launcher/driver transaction:** the same root custodian issues a one-shot authenticated delegation for the exact child it spawns, binding resource key, transaction/generation, action/argv digest, installed driver digest, inherited private channel, host PID plus retained pidfd/start identity, UID and cgroup. Root authenticates kernel per-message `SCM_CREDENTIALS` with `SO_PASSCRED` on the inherited channel (not socketpair creator `SO_PEERCRED`) and consumes the delegation before stop-before-exec release of that child; only its closed action/cleanup scope joins the active BEGIN. No env nonce, bearer string, ambient WeakMap, PID alone or `alreadyLocked`/`ignorePending` flag suffices. Child completion is an intermediate result, never an independent COMMIT; driver success followed by launcher failure stays pending. Direct/unrelated invocations cannot join or bypass it, including after restart: admitted standalone drivers use the same external resource exclusion and their own distinct transaction only when complete history is settled; no custodian means denial before tools/effects.
- **HOLD 4 — worker cleanup publication:** root admits the exact worker and delegates only one cleanup-receipt submission over the custodian lease socket, bound to `SO_PEERCRED` on its own connection, retained host PID/pidfd, container/cgroup, source, state, transaction/generation, one-shot nonce and expected cleanup action. The worker neither resolves/creates production state nor writes root control files; after its real termination cleanup it submits the bounded exact receipt. Custodian validates and consumes the delegation, durably publishes the root-owned receipt and acknowledges it. Retain the receipt channel until acknowledgment or sticky failure. Parent independently validates the custodian-authenticated receipt/acknowledgment over its bootstrap-authenticated retained channel, exact worker/delegation/outcome and actual worker settlement before continuing retirement. A receipt is not COMMIT; forged, copied, replayed, foreign, late or lost publication/acknowledgment taints the outer transaction. No process-local scope bypass or unauthenticated parent-synthesized success substitutes for this cross-process protocol.
- **Production ingress/test separation:** legacy launcher without an admitted custodian denies before its first effect, even as root. CLI, direct state/acquisition/control/temp helpers, core/operations, supervisor start/stop, profile invocation, worker entry/publication and both insecure-driver Python cleanup paths must route through authenticated custodian admission; defensive mutation/retirement checks remain. Pure inspection is read-only; inventory reports pending independently of phase/recovery or fails closed. Pure/test runtime instances have separate non-production state, coherent modeled ownership and fault injection, never production authority. No request/options/environment/CLI selector, mutable setter, caller predicate, `allowNonRoot` or test-issued object switches production binding. The controlled product route remains supported only through the protected owner, not by weakening legacy gates.
- **Retirement order/ownership:** await the full outer operation, all child results and every callback/finally cleanup; exact descriptor-bound quarantine/move/revalidation; both `docker_tool_custody` and `file_custody` retirement; all file/control/temp and empty-directory/state/lock removals; final absence checks, parent/directory fsyncs and every retirement-related handle close. Only root's outer owner then publishes additive COMMIT. Inner helpers borrow exactly owned descriptors, never commit or close another owner's handle; await all closures rather than fail-fast aggregation. Swallowed inner failure permanently taints settlement. Unknown/mismatched files AND empty directories remain preserved; catch-time mkdir is not crash durability. Clear/remove, including already-absent clear, borrow only externally authenticated exact active delegation or request a distinct owning transaction; expired/cross-state/cross-runtime/concurrently reused delegation refuses. Acquisition uncertainty and consume-before-invocation one-shot destroy remain independent; COMMIT never authorizes retired-authority replay/adoption.
- **Literal enumeration:** replace `compgen -G` in the insecure driver with descriptor-relative literal directory enumeration and literal prefix matching, including both Python cleanup paths and journal discovery. Paths containing glob metacharacters are ordinary literal names; enumeration errors/overflow veto. Retained successful custody requires authenticated committed status, never mere existence; unresolved/legacy custody remains pending.
- **Commit uncertainty:** all resource-retirement obligations finish before terminal publication; publication's own write/fsync/close, including external journal-exclusion handle release, is the finite commit boundary. Report success only after durable COMMIT acknowledgment; any publication error fails that invocation and retains history, possibly pending, malformed or complete COMMIT with uncertain acknowledgment. No catch-time retry/delete/rollback/replacement receipt repairs that failure, and no permanent-veto promise is made for every publication error. A fresh exclusive root reader must validate exact BEGIN/history/protected identities and establish durability of terminal file, containing directory and relevant ancestors with successful handle settlement; readable COMMIT alone is insufficient. Only complete durably established COMMIT may admit a later operation, never turn the failed invocation successful.
- **Pending means crash-safe refusal:** no automatic recovery, retirement replay/resume or retry of uncertain driver destroy is authorized. Loss of parent, custodian, lease or acknowledgment cannot erase pending across state/lock removal or process restart; preserve evidence for separately authorized operator intervention. Bootstrap/host replacement is not permission to reuse old resource coordinates or relabel an uncertain generation. Deadlines still trigger bounded receipt-owned containment, never prove clean retirement.
- **Required portable closure tests (not implementation/execution claims):** nonroot, missing UID/namespace API, userns/container root, unsafe ancestry/chowned custody/writable aliases, wrong candidate inventory/tool/closure and startup-hook injection refuse before effects. Fault installation and every reservation/BEGIN write/fsync/close; pending history and unusable journal must leave main/state mkdir/spawn/driver sentinels untouched. Preserve old-source replacement coverage and test actual quarantine unlink/rmdir foreign-file/empty-directory boundaries, literal metacharacters and enumeration errors in both cleanup paths; coherent syscall models retain every safety assertion. Separately gated protected Linux success must exercise repeated clean generations with retained receipts.
- **Required cross-process/crash tests:** exact child delegation, unrelated/direct driver exclusion after inner success before outer COMMIT and fresh-process restart after outer failure; forged/replayed/expired/cross-state/concurrent delegation; successful separate-process worker cleanup submission and parent validation while BEGIN is pending, plus foreign peer/wrong receipt/lease loss/ack loss refusal. Direct/scoped clear/remove and already-absent clear, swallowed failure/sticky taint, one close owner and awaited-all closure failures; faults before/after callback/finally, removal, lock release, every fsync/close and COMMIT write/fsync/close; competing admission in lock-release/publication gap, removed state with pending/newer/malformed history, uncertain complete COMMIT and fresh-reader durability failure; inventory veto. Fixtures cannot skip safety or select production test bypasses.
- **Exact path admission:** add only existing `dev/launcher/main.ts`, `dev/launcher/control.ts`, `dev/launcher/worker-entry.ts`, `dev/launcher/worker-process.ts`, `test/dev-launcher-control-otlp.test.ts`, `test/dev-launcher-worker-process.test.ts` to lifecycle's product matrix: 78 paths, still eight previously allocated new files. All retain historical Q owners; `main.ts` is newly allocated lifecycle with zero remediation-to-Q gross additions. Custodian/runner adapters use already allocated `dev/product-test/host-custody.py`, `dev/product-test/runner.ts`, `dev/product-test/snapshot-owner.ts`; portable root models extend existing `test/production-compose.test.ts`. Workflow bootstrap edits remain outside this source matrix and require the later independently reviewed existing-file execution/path gate and checker synchronization; no current workflow admission is implied.
- **Measured prospective replan:** at `cc58ba37`, Q-relative completion 2,552, relay 1,771, lifecycle 3,984, integration 4,475, total 12,782; integration's former 25 remaining cannot fund an external owner. Change only prospective completion forecast 3,500→3,000 and integration 4,500→5,000, total still <=15,100; integration cumulative high 17,000→17,325 covers Q 12,325 + 5,000. No historical gross/owner transfers, reserve release or deletion/compression credit. Remaining 2,318 before this governance diff: completion 448 for event/telemetry corrections/tests; relay 1,029 for bounded KVM closure/tests; lifecycle 316 = ingress/delegation adapters 90 + retirement 75 + existing-file tests/fixture migration 125 + contingency 26; integration 525 = bootstrap/ledger 200 + shared transaction/lease protocol 100 + runner adapters 40 + tests/docs/budget/later gate/regeneration 160 + contingency 25. Governance consumes these amounts; they are forecasts, not demonstrated fit or permission to omit inherited tasks. Stop for reviewed replan if complete implementation/tests cannot fit. Every other owner high, byte/global/physical cap, anchor and execution stop remains independent and unchanged.
- **This commit:** revise only this existing ADR (<=121 lines), same-line README synchronization, measured config and its existing budget checker/tests; run local central budget, targeted budget/docs tests and docs/format/diff checks, then commit plan only. No product implementation, new tracked files, readiness regeneration, source-complete/finding-closure claim, reserve release or Docker/KVM/AWS/provider/local execution; no workflow dispatch or authoritative H/G/Q operation.
## Historical governance-gate enforcement and validation boundary
The budget checker/tests must enforce actual Q-relative owner line+byte highs, global and every independent cap, the exact eight-file allocation, no ownership transfer/deletion credit and hostile central/attribute bypasses. Preserve raw UTF-8 added-line bytes including actual LF/CRLF, full canonical generated-line charges, ordinary untracked-file charges, and NUL/UTF-8/ordinary-file checks. Preserve isolated Git configuration/attributes, uncached index, explicit repository-transform rejection, Myers/indent/U3 patch selection and numstat cross-check; no filter, textconv, rename/copy or staged-clean-output bypass. Historical ADR0334 accounting details remain applicable.
Only this ADR, `docs/adr/README.md`, the ADR0334 supersession reference, `config/external-review-remediation-budget-v1.json`, `scripts/check-stage2-retained-lines.py` and `test/stage2-remediation-budget.test.ts` change in this governance gate. Run local budget measurement against actual current integrated usage, targeted budget tests and formatting/diff checks. No full check, readiness regeneration, source implementation, Docker/KVM execution or finding closure is claimed. The original pre-commit instruction was “Do not commit pending two independent reviews”; the gate's committed status does not establish final source implementation review. No AWS/provider/model/credential operation or authoritative H/G/Q workflow is authorized; no retirement or evidence relabeling. **Stop immediately before every AWS-facing command.**
