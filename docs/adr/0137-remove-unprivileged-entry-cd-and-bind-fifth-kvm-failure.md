# ADR 0137: Remove unprivileged entry cd and bind fifth KVM failure

- Status: Accepted under the owner's explicit standing authorization for all non-AWS prerequisite work
- Date: 2026-08-23

Run `32613383776`, attempt 1, passed stable history admission, exact H/G acquisition, static-control validation, immutable preparation, and installed-runtime checks. The sole entry shell then attempted to `cd` as the unprivileged runner into the root-owned mode-0700 fixed source and failed before Python or KVM entry. Recovery, fixed cleanup, independent residue, and output cleanup all passed. No artifact was produced and the observation grants no KVM, qualification, retry, promotion, or release claim.

Private failure-custody SHA-256 is `c0622beb22138940c27d99ac07bcaf5d75b558e40f084a350f70040dc9fd5b2d`.

The privileged Python entry already uses an absolute authenticated source path and inserts that exact absolute module path. It does not require the runner shell's current directory. Remove only the unprivileged `cd`; do not weaken fixed-source ownership or traversal permissions. Bind run `32613383776` as the fifth exact completed/failure predecessor and raise the closed history to six rows for one distinct corrected observation. Update the reviewed workflow digest and retain attempt-one/no-rerun admission.

The owner's repeated instruction authorizes completion of all non-AWS prerequisites without another prompt. This accepts one distinct corrected local observation; it is not a rerun and cannot rewrite the failed predecessor. Any outcome consumes that observation.

This decision grants no AWS/API/credential/provider/OpenTofu/SSM/inventory/campaign, deployment, production, promotion, or release authority.
