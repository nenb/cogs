# ADR 0193: Refresh the V3 parser binding before the next lifecycle

- Status: Accepted under the owner's explicit standing authorization for all non-AWS prerequisite work
- Date: 2026-08-26

The complete local suite identified a stale operation-level source hash after the accepted V3 guest normalization. Refresh that exact parser-source binding and update the production fixture to provide the already-required durable launch namespace/rootfs object. The production network and SSH matrices pass in normal and optimized Python modes.

H35 was canceled before runtime launch because its fixed source preceded this binding correction. Its ordinary recovery, settlement, and independent residue stages all returned zero. It minted no qualification.

No runtime guarantee is weakened, and this grants no AWS or qualification authority.
