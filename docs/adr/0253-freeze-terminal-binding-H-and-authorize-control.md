# ADR 0253: Freeze terminal-binding H and authorize its control

- Status: Accepted under the owner's explicit standing authorization for all non-AWS prerequisite work
- Date: 2026-08-30

Freeze the corrected implementation H as protected-main revision `bf0479a012b39c074ecb623ea83e85b3dc3ebe36`. PR #451 passed complete quality, image/SBOM, secret, and Linux-foundation checks, including the native `network-runtime` shard. Focused static-admission, evidence, coordinator, hostile guard, readiness, and complete portable suites passed locally. The correction is limited to completing the exact host-attestation field from the authenticated held runtime manifest; it does not change runtime effects, production deadlines, cleanup, evidence authority, or AWS scope.

The successful no-mint diagnostic `33312269679` proved that the preceding real lifecycle reached terminal evidence only after seven samples, 21 measurements, exact teardown, and zero residue. It minted no receipt and grants no qualification claim.

Advance the authenticated static dispatch guard to V23. Bind successful predecessor run `33290514426`, attempt 1, workflow head `b6a6f4613ded73d0496602eb5dd9b5db7520e04a`, and prior H `89243c8d9f7a946aefdaa4c445a5cfe1e0fe7e14` as preserved non-authorizing history. Require protected `main`, exact H `bf0479a012b39c074ecb623ea83e85b3dc3ebe36`, complete authenticated history, earliest/current cardinality, and the existing static-only process/descriptor boundary.

After this independently reviewed G workflow merges, authorize exactly one attempt-1 no-KVM static-control observation for H. It remains non-authoritative data and grants no retry, qualification, promotion, AWS, provider, deployment, campaign, production, or release operation. Raise only the complete tracked-source cardinality bound from 1,214 to exactly 1,215 files; retain all other bounds.
