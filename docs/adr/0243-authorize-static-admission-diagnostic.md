# ADR 0243: Authorize a static-admission diagnostic

- Status: Accepted under the owner's explicit standing authorization for all non-AWS prerequisite work
- Date: 2026-08-30

Formal local qualification run `33292919137`, attempt 1, passed protected-main pre-admission, exact H/G checkout, immutable preparation, recovery, fixed cleanup, and independent zero-residue settlement. The zero-argument entry then returned blocked status 3 before creating an operation or canonical report. No report or receipt artifact was uploaded, and the run grants no qualification, retry, or promotion claim.

Private Osito reproduction with exact H, staged control bytes, cached immutable inputs, and the same package-private static-custody call passed. Authorize one bounded protected-main diagnostic workflow that repeats only exact source/control/immutable preparation and the static-custody claim on a fresh hosted runner. It must not invoke the lifecycle entry, KVM, Kata launch, SSH, QMP, report publication, qualification, or any provider. Emit only bounded exception classes/messages, close any claimed custody, and remove `/var/lib/cogs` and `/opt/kata` on every outcome.

The new workflow and this decision raise only the complete tracked-source cardinality bound from 1,203 to exactly 1,205 files. All byte bounds, complete inventory rules, and generated-evidence exclusions remain unchanged. The diagnostic grants no replacement qualification by itself and no AWS, provider, deployment, campaign, production, or release operation.
